name: Deploy All Services (CI)

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      CONTA_SERVICE_PATH: ./conta-service
      KAFKA_SERVICE_PATH: ./kafka-service
      INFRA_PATH: ./infra
      DOCKER_BUILDKIT: 1
      COMPOSE_DOCKER_CLI_BUILD: 1

    steps:
      # ======================================
      # 1️⃣ Checkout do repositório
      # ======================================
      - name: Checkout do código
        uses: actions/checkout@v4

      # ======================================
      # 2️⃣ Configurar Java para builds dos serviços Spring Boot
      # ======================================
      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      # ======================================
      # 3️⃣ Build do conta-service (Spring Boot)
      # ======================================
      - name: Build do conta-service
        working-directory: ${{ env.CONTA_SERVICE_PATH }}
        run: |
          echo "🏗️  Buildando conta-service..."
          ./mvnw clean package -DskipTests
          echo "✅ Build concluído para conta-service"

      # ======================================
      # 4️⃣ Build do kafka-service (Spring Boot)
      # ======================================
      - name: Build do kafka-service
        working-directory: ${{ env.KAFKA_SERVICE_PATH }}
        run: |
          echo "🏗️  Buildando kafka-service..."
          ./mvnw clean package -DskipTests
          echo "✅ Build concluído para kafka-service"

      # ======================================
      # 5️⃣ Configurar Docker e Docker Compose
      # ======================================
      - name: Configurar Docker Compose
        run: |
          docker version
          docker compose version

      # ======================================
      # 6️⃣ Subir os containers com variáveis de ambiente do .env
      # ======================================
      - name: Subir containers (infra + serviços)
        working-directory: ${{ env.INFRA_PATH }}
        run: |
          echo "🚀 Subindo containers definidos em docker-compose.yml"
          docker compose --env-file .env up -d --build
          echo "✅ Containers iniciados"
          docker ps -a

      # ======================================
      # 7️⃣ Aguardar serviços estarem prontos
      # ======================================
      - name: Aguardar PostgreSQL e Zookeeper ficarem 'healthy'
        run: |
          echo "⏳ Aguardando PostgreSQL e Zookeeper ficarem prontos..."
          for i in {1..30}; do
            STATUS_DB=$(docker inspect --format='{{.State.Health.Status}}' postgres-db 2>/dev/null || echo "none")
            STATUS_ZK=$(docker inspect --format='{{.State.Health.Status}}' zookeeper 2>/dev/null || echo "none")
            echo "Tentativa $i → postgres-db=$STATUS_DB, zookeeper=$STATUS_ZK"
            if [[ "$STATUS_DB" == "healthy" && "$STATUS_ZK" == "healthy" ]]; then
              echo "✅ Ambos os serviços estão saudáveis!"
              break
            fi
            sleep 5
          done

      # ======================================
      # 8️⃣ Exibir logs detalhados dos serviços
      # ======================================
      - name: Exibir logs detalhados
        run: |
          echo "🔍 Logs detalhados dos serviços:"
          docker ps -a
          echo "=== conta-service logs ==="
          docker logs conta-service || echo "⚠️ conta-service não encontrado"
          echo "=== kafka-service logs ==="
          docker logs kafka-service || echo "⚠️ kafka-service não encontrado"
          echo "=== postgres-db logs ==="
          docker logs postgres-db || echo "⚠️ postgres-db não encontrado"
          echo "=== kafka-broker logs ==="
          docker logs kafka-broker || echo "⚠️ kafka-broker não encontrado"
          echo "=== zookeeper logs ==="
          docker logs zookeeper || echo "⚠️ zookeeper não encontrado"

      # ======================================
      # 9️⃣ Encerrar containers após o CI
      # ======================================
      - name: Encerrar containers
        if: always()
        working-directory: ${{ env.INFRA_PATH }}
        run: |
          echo "🧹 Encerrando containers..."
          docker compose down -v
          echo "✅ Limpeza concluída."