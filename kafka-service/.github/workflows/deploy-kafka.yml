name: Deploy Kafka Service

on:
  push:
    branches: [ main ]
    paths: [ 'kafka-service/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'kafka-service/**' ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: "[1] Checkout do código"
        uses: actions/checkout@v4

      - name: "[2] Configurar JDK 21"
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: "[3] Build Projeto Raiz"
        run: mvn clean install -DskipTests

      - name: "[4] Build e Testes Kafka Service"
        run: |
          cd kafka-service
          mvn clean compile test package

      - name: "[5] Docker Build"
        run: |
          cd kafka-service
          docker build -t kafka-service:latest .

      - name: "[6] Integration Test"
        run: |
          cd infra
          docker compose up -d kafka-service postgres kafka-broker zookeeper
          sleep 45
          curl -f http://localhost:8082/actuator/health || exit 1
          docker compose down

      - name: "[7] Deploy (Placeholder)"
        if: github.ref == 'refs/heads/main'
        run: echo "🚀 Deploy para produção aqui..."