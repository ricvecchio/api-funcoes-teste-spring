version: "3.9"

services:
  # 🐘 Banco de dados PostgreSQL
  postgres-db:
    image: postgres:15
    container_name: postgres-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432" # Mapeia a porta do host para o container
    volumes:
      - postgres_data:/var/lib/postgresql/data # Volume persistente do banco

  # 🐦 Zookeeper (necessário para o Kafka)
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: ${ZOOKEEPER_PORT}
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "${ZOOKEEPER_PORT}:${ZOOKEEPER_PORT}" # Usa variável para maior flexibilidade

  # 🥨 Kafka broker principal
  kafka-broker:
    image: confluentinc/cp-kafka:7.3.0
    container_name: kafka-broker
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:${ZOOKEEPER_PORT}
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka-broker:${KAFKA_BROKER_PORT}
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
    ports:
      - "${KAFKA_BROKER_PORT}:${KAFKA_BROKER_PORT}"
    command:
      - sh
      - -c
      - |
        echo "🕒 Esperando o Zookeeper ficar pronto..."
        # Aguarda até 30 tentativas de conexão com o Zookeeper antes de iniciar o Kafka
        /bin/bash -c 'for i in {1..30}; do nc -z zookeeper ${ZOOKEEPER_PORT} && break; echo "⏳ Tentando conectar ao Zookeeper... ($i/30)"; sleep 3; done'
        echo "✅ Zookeeper pronto! Iniciando Kafka..."
        /etc/confluent/docker/run

  # 🏦 Conta Service (microserviço REST principal)
  conta-service:
    build:
      context: ../conta-service
      dockerfile: Dockerfile
    image: conta-service:latest
    container_name: conta-service
    depends_on:
      - postgres-db
      - kafka-broker
    environment:
      # Configuração de banco de dados (via variáveis .env)
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-db:${DB_PORT}/${DB_NAME}
      SPRING_DATASOURCE_USERNAME: ${DB_USER}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}

      # Configuração de Kafka
      KAFKA_BOOTSTRAP_SERVERS: kafka-broker:${KAFKA_BROKER_PORT}

    ports:
      - "${CONTA_SERVICE_PORT}:8080" # Porta interna fixa (8080), externa configurável via variável

    command:
      - sh
      - -c
      - |
        echo "🕒 Esperando Kafka e Postgres ficarem prontos..."
        # Garante que dependências estejam disponíveis antes de iniciar
        /bin/bash -c 'for i in {1..30}; do nc -z kafka-broker ${KAFKA_BROKER_PORT} && nc -z postgres-db ${DB_PORT} && break; echo "⏳ Aguardando dependências... ($i/30)"; sleep 3; done'
        echo "✅ Dependências disponíveis! Iniciando aplicação Spring Boot..."
        java -jar /app/app.jar

  # 🔄 Kafka Service (processamento assíncrono)
  kafka-service:
    build:
      context: ../kafka-service
      dockerfile: Dockerfile
    image: kafka-service:latest
    container_name: kafka-service
    depends_on:
      - kafka-broker
    environment:
      # Configuração de Kafka via variável do .env
      KAFKA_BOOTSTRAP_SERVERS: kafka-broker:${KAFKA_BROKER_PORT}

      # Configuração de banco de dados via variável (mesma estrutura que conta-service)
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-db:${DB_PORT}/${DB_NAME}
      SPRING_DATASOURCE_USERNAME: ${DB_USER}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}

    ports:
      - "${KAFKA_SERVICE_PORT}:8080" # Porta interna fixa (8080), externa configurável via variável

    command:
      - sh
      - -c
      - |
        echo "🕒 Aguardando Kafka e Postgres ficarem prontos..."
        /bin/bash -c 'for i in {1..30}; do nc -z kafka-broker ${KAFKA_BROKER_PORT} && nc -z postgres-db ${DB_PORT} && break; echo "⏳ Aguardando dependências... ($i/30)"; sleep 3; done'
        echo "✅ Dependências disponíveis! Iniciando serviço Kafka..."
        java -jar /app/app.jar

# Volume persistente para o banco
volumes:
  postgres_data: